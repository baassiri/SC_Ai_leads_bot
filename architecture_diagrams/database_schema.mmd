erDiagram

    lead_timeline {
        INTEGER id PK
        INTEGER lead_id NOT NULL
        TEXT action NOT NULL
        TEXT message
        TEXT details
        TEXT metadata
        TIMESTAMP timestamp NOT NULL
    }

    sqlite_sequence {
         name
         seq
    }
